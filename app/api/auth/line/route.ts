import { env } from "cloudflare:workers";import { NextResponse } from "next/server";import { cookies } from "next/headers";
export async function GET(){if(!env.LINE_LOGIN_CHANNEL_ID)return NextResponse.json({error:"LINE Login is not configured"},{status:503});const state=crypto.randomUUID();const nonce=crypto.randomUUID();const store=await cookies();store.set("line_oauth_state",state,{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:600});store.set("line_oauth_nonce",nonce,{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:600});const redirect="https://www.csasuspension.com/api/auth/line/callback";const url=new URL("https://access.line.me/oauth2/v2.1/authorize");url.searchParams.set("response_type","code");url.searchParams.set("client_id",String(env.LINE_LOGIN_CHANNEL_ID));url.searchParams.set("redirect_uri",redirect);url.searchParams.set("state",state);url.searchParams.set("scope","openid profile");url.searchParams.set("nonce",nonce);return NextResponse.redirect(url)}